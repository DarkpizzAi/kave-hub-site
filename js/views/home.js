// Home page: the countdown to the new flat lands here in the next batch.

import { emptyNote } from '../ui.js';

export default async function home(container) {
  container.append(emptyNote('The countdown is on its way.'));
}
